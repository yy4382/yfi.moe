---
interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---

<div
  class:list={`bg-card shape-card text-heading ${className}`}
>
  <slot name="heading" />
  <slot />
</div>
