---
import { GET } from "src/pages/api/note/[id]";
interface Props {
  pageId: string;
}
const { pageId } = Astro.props;
const response = await GET({
  ...Astro,
  params: { ...Astro.params, id: pageId },
});
const pageContent = await response.json();
---

<Fragment set:html={pageContent} />
