---
interface Props {
  pageId: string;
}
const { pageId } = Astro.props;
const response = await fetch(`/api/note/${pageId}`);
const pageContent = await response.json();
---

<Fragment set:html={pageContent} />
