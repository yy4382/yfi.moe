---
import Card from "@comp/elements/Card.astro";
interface Props {
  cardTitle: string;
  cardLink: string;
}
const { cardTitle, cardLink } = Astro.props;
---

<Card class="w-full" useHeading leftExtraPadding padding="xs">
  <a
    href={cardLink}
    slot="heading"
    class="transition mb-2 block hover:text-primary"
  >
    <h5>
      {cardTitle}
    </h5>
  </a>
  <slot />
</Card>
