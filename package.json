{
  "name": "blog",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "keywords": [],
  "license": "ISC",
  "author": "",
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "build:vercel": "git submodule init && turbo run fetch-article && turbo run build",
    "check-types": "turbo run check-types",
    "dev": "turbo run dev",
    "format": "turbo run format",
    "lint": "turbo run lint"
  },
  "lint-staged": {
    "*": "turbo run format"
  },
  "devDependencies": {
    "husky": "^9.1.6",
    "lint-staged": "^15.2.10",
    "prettier": "^3.3.3",
    "turbo": "^2.2.3",
    "typescript": "^5.6.3",
    "@eslint/plugin-kit": ">=0.2.3"
  },
  "packageManager": "pnpm@9.12.2",
  "pnpm": {
    "overrides": {
      "@iconify/tools": "^4.0.7"
    }
  }
}
