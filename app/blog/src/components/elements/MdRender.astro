---
import { cn } from "@utils/cn";

interface Props {
  class?: string;
}
const props = Astro.props;
---

<div
  class:list={cn([
    "mx-auto prose break-words prose-gray dark:prose-invert prose-headings:scroll-mt-8",
    props.class,
  ])}
>
  <slot />
</div>
